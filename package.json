{
  "name": "palisade-inbox-bot",
  "version": "1.0.0",
  "description": "A minimal Slack bot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "dependencies": {
    "@slack/bolt": "^3.12.2",
    "dotenv": "^16.0.3"
  },
  "private": true
}
